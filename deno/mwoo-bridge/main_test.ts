// import { assertArrayIncludes, assertObjectMatch } from "@std/assert";
// import { moo, woo } from "./main.ts";

// Deno.test(async function mooTest() {
//   const requiredFunctions = [
//     "core_course_get_courses",
//     "core_webservice_get_site_info",
//   ];
//   const siteInfo = await moo.core.webservice.getSiteInfo();

//   assertArrayIncludes(
//     siteInfo.functions.map(({ name }) => name),
//     requiredFunctions,
//   );
// });

// Deno.test(async function wooTest() {
//   assertObjectMatch(await woo.get("products"), { status: 200 });
// });
